---
version: "1.0.0"
date: 2026-07-29
author: AKO_studio
status: production
tags: [知识管理, 向量检索, 语义搜索, embedding]
---

# AKO 知识库 Agent 白皮书

> **AKO体系知识管理与检索Agent**
> 基于向量数据库与混合检索架构，为AKO所有Agent提供统一的知识存储、检索与同步能力

---

## 一、定位与使命

### 1.1 定位

AKO_knowledge（知识库Agent）是AKO Agent体系的**知识中枢**。它通过向量化存储、混合检索策略和自动化数据摄入管道，为所有AKO Agent提供统一、高效、语义精准的知识检索服务。

### 1.2 使命

- **统一知识管理**：集中管理AKO体系的企业知识资产（规范文档、设计资料、项目经验等）
- **智能语义检索**：提供基于向量和关键词的混合检索，精准匹配用户知识需求
- **自动知识摄入**：支持多种数据源（Obsidian Vault、本地文件Ima、PDF文档）的自动化摄入
- **知识持续演进**：通过定期重索引、数据发酵和版本对比，保持知识库的时效性和准确性

### 1.3 适用场景

| 场景 | 说明 |
|------|------|
| 规范标准检索 | 快速查询建筑设计规范、消防标准、节能规范等 |
| 设计经验查询 | 检索历史项目的设计笔记、技术方案、问题记录 |
| 知识库初始化 | 从Obsidian知识库或其他源批量导入知识 |
| 增量知识更新 | 新增文件自动向量化并入库 |
| 知识一致性校验 | 检查本地知识与云端知识是否同步 |

---

## 二、核心能力

| 能力ID | 名称 | 说明 | 优先级 |
|--------|------|------|--------|
| capability_001 | 混合检索 | 支持向量语义检索与关键词检索的混合策略，返回最相关结果 | P0 |
| capability_002 | 知识摄入 | 从Obsidian Vault、PDF、IMA知识库等多源自动摄入数据 | P0 |
| capability_003 | 向量存储与管理 | 基于ChromaDB的向量化存储，支持增删改查与索引维护 | P0 |
| capability_004 | 知识发酵 | 夜间/定时执行知识优化，自动合并重复、拆解复杂文档 | P1 |
| capability_005 | 知识同步 | 检查本地知识库与云端的一致性，差异同步 | P1 |
| capability_006 | 配置热加载 | 支持运行时切换embedding模型、检索参数等配置 | P1 |

### 能力定义详述

**Capability: 混合检索**
- **输入**：查询文本、过滤条件（domain/tags/date范围）、top_k参数
- **输出**：排序后的检索结果列表（含文档ID、相关性评分、内容片段）
- **核心算法**：向量余弦相似度 + BM25关键词评分 + 加权融合

**Capability: 知识摄入**
- **输入**：数据目录路径或文件列表
- **输出**：摄入统计（处理文件数、成功数、失败数、向量条数）
- **支持格式**：Markdown、PDF、TXT、JSON

---

## 三、系统架构

### 3.1 整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                      AKO_knowledge Agent                       │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ 摄入管道     │  │ 检索引擎    │  │ 维护管理    │          │
│  │             │  │             │  │             │          │
│  │ ingest_    │  │ query_     │  │ maintain   │          │
│  │ pdf/obsidian│  │ enhanced / │  │ migrate /  │          │
│  │_vault / hub│  │ hybrid     │  │ validate   │          │
│  │_all         │  │_retrieval  │  │_config     │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
│         │                │                │                  │
│         ▼                ▼                ▼                  │
│  ┌──────────────────────────────────────────────────────┐    │
│  │                  ChromaDB 向量数据库                    │    │
│  │  (多个collection: standards / projects / design_notes等)│   │
│  └──────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ config.json │  │ config_    │  │ data/       │          │
│  │ (主配置)    │  │ loader.py  │  │ gbrain_     │          │
│  │             │  │ (配置管理)  │  │ graph.json  │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 语言 | Python 3.11+ | 核心开发语言 |
| 向量数据库 | ChromaDB | 轻量级本地向量存储 |
| Embedding | Nomic text-embedding | 默认embedding模型 |
| 配置管理 | JSON + Python | 可配置多种检索模式 |
| 检索策略 | 全局向量/混合检索 | 按场景自适应切换 |

### 3.3 检索策略

| 策略 | 原理 | 适用场景 |
|------|------|---------|
| 全局向量检索 | 对整个知识库做向量相似度搜索 | 通用知识查询 |
| 混合检索 | 向量语义搜索 + BM25关键词加权融合 | 精准定位需求 |
| 知识发酵 | 夜间自动优化文档切分与向量索引 | 知识质量提升 |

---

## 四、接口定义

### 4.1 核心API

| 接口 | 方法 | 说明 |
|------|------|------|
| query | gbrain query | 自然语言查询知识库 |
| search | gbrain search | 关键词全文搜索 |
| ingest | python ingest_*.py | 自动摄入知识文档 |
| maintain | python maintain.py | 知识库维护操作 |

### 4.2 与AKO体系集成

| 交互对象 | 交互内容 | 方式 |
|---------|---------|------|
| AKO_architect_agent | 提供规范/设计知识检索 | REST API |
| AKO_hub | 接收知识管理任务 | REST API |
| AKO_registry_agent | 自注册与心跳上报 | SDK集成 |

---

## 五、工作流程

### 5.1 知识检索流程

```
用户查询请求
  ↓
查询解析（query_enhanced.py）
  ├─ 语义理解 → 提取关键概念
  └─ 意图分类 → 确定搜索域
  ↓
混合检索执行（hybrid_retrieval.py）
  ├─ 向量检索 → ChromaDB语义搜索
  ├─ 关键词检索 → BM25全文搜索
  └─ 结果融合 → 加权排序
  ↓
结果后处理
  ├─ 相关性排序
  ├─ 去重与聚合
  └─ 引用标注
  ↓
返回检索结果
```

### 5.2 知识摄入流程

```
选择数据源 (Obsidian Vault / IMA / PDF / Hub)
  ↓
文件扫描 → 识别支持格式
  ↓
文档预处理 → 切分（按语义段落/标题层级/固定长度）
  ↓
向量化 → 调用embedding模型生成向量
  ↓
存储到ChromaDB → metadata标注（来源/日期/分类）
  ↓
更新索引 → 维护倒排索引
  ↓
完成摄入
```

---

## 六、部署与运维

### 6.1 部署要求

| 要求 | 说明 |
|------|------|
| Python版本 | 3.11+ |
| 依赖安装 | pip install -r requirements.txt |
| 配置准备 | 编辑 config.json，配置embedding模型和检索参数 |
| 向量库初始化 | 运行摄入脚本建立初始知识库 |
| 启动方式 | 通过知识服务API对外提供检索能力 |

### 6.2 配置说明

| 配置项 | 类型 | 说明 |
|--------|------|------|
| embedding_model | string | Embedding模型名称 |
| chroma_path | string | ChromaDB持久化路径 |
| top_k | int | 默认返回结果数 |
| hybrid_weight | float | 混合检索权重（向量:关键词） |
| ingest_sources | list | 自动摄入的源路径列表 |

### 6.3 健康检查

| 探针 | 检查内容 | 超时（秒） |
|------|---------|-----------|
| 核心功能自检 | 执行一次测试检索 | 30 |
| 输入输出通道 | 验证文件读写 | 10 |
| 依赖服务可用 | 验证embedding服务 | 5 |
| 知识库向量完整性 | 检查collection状态 | 10 |
| 配置有效性 | 验证config.json | 5 |

---

> **文档版本**: v1.0.0  
> **创建日期**: 2026-07-29  
> **作者**: AKO_studio  
> **审核状态**: production
