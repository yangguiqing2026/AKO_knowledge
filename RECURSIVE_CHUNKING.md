# 递归式切片(Recursive Chunking)说明

## 🎯 什么是递归式切片?

递归式切片是一种智能的文本分块策略,它会**优先在语义边界处切分**,而不是简单地按固定字符数切割。

## 📊 对比传统切片

### 传统切片(固定长度)
```python
# 768字符一切割,可能在句子中间切断
文本: "...这是一个完整的句子。这是下一个句子的开始..."
切片1: "...这是一个完整的句子。这是下一"  ❌ 句子被切断
切片2: "个句子的开始..."
```

### 递归式切片(智能边界)
```python
# 优先在句号、段落等处切分
文本: "...这是一个完整的句子。这是下一个句子的开始..."
切片1: "...这是一个完整的句子。"  ✅ 完整句子
切片2: "这是下一个句子的开始..."
```

## 🔧 工作原理

### 分割优先级(从高到低)

1. **段落分隔** (`\n\n`) - 最高优先级
2. **换行** (`\n`)
3. **中文标点** (。！？；，)
4. **英文标点** (. ! ? ; , + 空格)
5. **硬切分** (最后手段,按字符数)

### 递归过程

```
输入文本(2000字符)
    ↓
尝试按段落(\n\n)分割
    ↓
得到3个段落: [800字, 600字, 600字]
    ↓
第1段800字 > 768限制 → 继续递归
    ↓
尝试按换行(\n)分割
    ↓
得到2个子段落: [400字, 400字] ✅ 都<768
    ↓
最终结果: [400字, 400字, 600字, 600字]
```

## 💡 核心优势

### 1. 保持语义完整性
- ✅ 不会在句子中间切断
- ✅ 保持段落结构
- ✅ 保留上下文关系

### 2. 自适应不同文档
- PDF: 按段落和页面分割
- Word: 按段落和表格分割
- PPT: 按幻灯片分割
- 图片OCR: 短文本不分块

### 3. 减少碎片化
- 合并过小的块(<614字符)
- 避免产生大量无意义的小片段

## 📝 代码结构

```python
chunk_text(text)
    └─> chunk_text_recursive(text)
            └─> _recursive_split(text, level=0)  # 递归分割
                    ├─> 按段落分割 (level=0)
                    ├─> 按换行分割 (level=1)
                    ├─> 按句号分割 (level=2)
                    ├─> ...
                    └─> hard_chunk()  # 最后手段
            └─> merge_small_chunks()  # 合并小块
```

## ⚙️ 配置参数

### 当前配置(config.json)
```json
{
  "common_settings": {
    "chunk_size": 768,      // 最大块大小
    "overlap": 256          // 重叠大小(硬切分时使用)
  }
}
```

### 可调参数

**减小 chunk_size** (更精细):
- 优点: 检索更精确
- 缺点: 片段更多,索引更大

**增大 chunk_size** (更粗粒度):
- 优点: 上下文更完整
- 缺点: 可能包含无关信息

## 🧪 测试示例

### 示例1: 普通文本
```python
text = "第一段内容。\n\n第二段内容。\n\n第三段很长的内容..." * 10

chunks = chunk_text(text)
print(f"原始长度: {len(text)}")
print(f"分块数量: {len(chunks)}")
print(f"平均长度: {sum(len(c) for c in chunks) / len(chunks):.0f}")
```

### 示例2: 带标题的文档
```python
text = """
# 第一章 引言

这是引言部分的内容...

# 第二章 方法

这是方法部分的内容...
"""

chunks = chunk_text(text)
# 会在标题处自然分割
```

## 🎯 适用场景

### ✅ 推荐使用
- 技术文档
- 学术论文
- 产品手册
- 长篇报告

### ⚠️ 注意事项
- 代码片段: 可能需要特殊处理
- 表格数据: 建议保持完整
- 列表项: 可能需要自定义分隔符

## 📈 性能影响

| 指标 | 传统切片 | 递归切片 |
|------|---------|---------|
| 分块速度 | 快 | 稍慢(可接受) |
| 检索精度 | 一般 | **更好** |
| 语义完整性 | 差 | **优秀** |
| 内存占用 | 低 | 略高 |

## 🔍 调试技巧

如果检索效果不理想,可以:

1. **检查分块结果**:
```python
chunks = chunk_text(your_text)
for i, chunk in enumerate(chunks):
    print(f"Chunk {i}: {len(chunk)} chars")
    print(chunk[:100])  # 预览前100字符
    print("---")
```

2. **调整参数**:
   - 检索太泛 → 减小 `chunk_size`
   - 检索太窄 → 增大 `chunk_size`
   - 碎片太多 → 增大 `merge_small_chunks` 阈值

3. **查看实际分割点**:
```python
# 在 _recursive_split 中添加日志
print(f"Level {level}: Split by '{separator}', got {len(parts)} parts")
```

## 🚀 下一步优化

如果需要进一步提升:

1. **基于模型的分割**: 使用LLM识别语义边界
2. **结构化感知**: 识别标题、列表、表格
3. **动态chunk_size**: 根据内容密度调整
4. **跨块关联**: 建立块之间的引用关系

---

**总结**: 递归式切片通过优先在语义边界处分割,显著提升了检索的相关性和准确性,是RAG系统的推荐实践!
