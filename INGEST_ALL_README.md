# 多格式知识库入库工具使用说明

## 📋 概述

`ingest_all.py` 是增强版的知识库入库工具,支持 **PDF、Word、PPT** 三种格式的文件自动入库。

## 🚀 快速开始

### 1. 准备文件夹结构

在配置的目录下创建以下文件夹:

```
D:\AG_docs\
├── pdfs\     # PDF 文件
├── word\     # Word 文档 (.docx)
└── ppt\      # PPT 演示文稿 (.pptx)
```

### 2. 放入文件

将需要入库的文件放入对应文件夹:
- `*.pdf` → `pdfs/`
- `*.docx` → `word/`
- `*.pptx` → `ppt/`

### 3. 运行入库

```bash
python ingest_all.py
```

## 📊 功能特性

### ✅ 支持的格式

| 格式 | 扩展名 | 提取内容 |
|------|--------|---------|
| PDF | `.pdf` | 文字 + 图片OCR |
| Word | `.docx` | 段落 + 表格 |
| PPT | `.pptx` | 逐页文字 |

### ✅ 智能处理

- **自动去重**: 已入库的文件会自动跳过
- **批量嵌入**: 提高处理效率
- **错误容错**: 单个文件失败不影响其他文件
- **进度显示**: 实时显示入库进度

### ✅ OCR 识别

PDF 中的图片会自动进行 OCR 识别,支持中英文混合。

## 🔧 配置说明

### 修改文件夹路径

编辑 `config.json`:

```json
{
  "profiles": {
    "computer_a": {
      "pdf_folder": "D:\\AG_docs\\pdfs",
      "word_folder": "D:\\AG_docs\\word",
      "ppt_folder": "D:\\AG_docs\\ppt"
    }
  }
}
```

### 切换电脑配置

```bash
# 查看当前配置
python switch_config.py

# 切换到电脑B
python switch_config.py computer_b
```

## 📝 输出示例

```
============================================================
知识库入库工具 (PDF + Word + PPT)
============================================================

当前配置: 电脑 A (computer_a)
  数据库路径: D:\AKO_knowledge
  PDF 文件夹: D:\AG_docs\pdfs
  Word 文件夹: D:\AG_docs\word
  PPT 文件夹: D:\AG_docs\ppt
  集合名称: ako_photos
============================================================
✅ 依赖检查通过

📂 PDF: 5 个文件
📂 Word: 3 个文件
📂 PPT: 2 个文件

共发现 10 个文件，开始处理...
============================================================
处理: 技术手册.pdf ...
  提取 25 段，开始入库...
  已入库 16/25
  已入库 25/25
[完成] 技术手册.pdf → 25 段

...

============================================================
全部完成!
  成功: 8 个文件
  跳过: 2 个文件
  共入库 156 段
============================================================
```

## ⚠️ 注意事项

1. **首次使用**: 确保安装了必要的依赖
   ```bash
   pip install python-docx python-pptx PyMuPDF Pillow pytesseract
   ```

2. **Tesseract OCR**: 需要单独安装 Tesseract-OCR
   - 下载地址: https://github.com/UB-Mannheim/tesseract/wiki
   - 安装后添加到系统 PATH

3. **文件大小**: 建议单个文件不超过 50MB

4. **内存占用**: 大量文件处理时注意内存使用

5. **百度云盘同步**: 
   - 入库完成后等待同步
   - 另一台电脑再运行查询

## 🔍 验证入库

```bash
# 查看数据库统计
python validate_config.py

# 查询测试
python query.py "你的问题"
```

## 🆚 与 ingest_pdf.py 的区别

| 特性 | ingest_pdf.py | ingest_all.py |
|------|--------------|---------------|
| PDF 支持 | ✅ | ✅ |
| Word 支持 | ❌ | ✅ |
| PPT 支持 | ❌ | ✅ |
| 自动分发 | ❌ | ✅ |
| 推荐使用 | 仅PDF场景 | 多格式场景 |

## 💡 最佳实践

1. **文件命名**: 使用有意义的文件名,便于追溯来源
2. **分类存放**: 按类型放入对应文件夹
3. **定期入库**: 新增文件后及时运行入库
4. **备份数据**: 重要文档保留原始文件
5. **检查日志**: 关注跳过的文件和错误信息

## 🐛 常见问题

### Q: Word/PPT 文件夹不存在怎么办?
A: 脚本会自动跳过不存在的文件夹,只处理存在的文件夹。

### Q: 如何重新入库已处理的文件?
A: 目前设计为自动去重,如需重新入库需手动删除数据库中的记录。

### Q: 支持 .doc 和 .ppt 旧格式吗?
A: 不支持,请先转换为 .docx 和 .pptx 格式。

### Q: OCR 识别不准确怎么办?
A: 可以尝试调整图片质量或使用更高分辨率的 PDF。
